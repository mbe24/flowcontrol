name: CI

on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]

jobs:
  web:
    name: Web (flowui · check · build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: flowui/package-lock.json

      - name: Install dependencies
        working-directory: flowui
        run: npm ci

      - name: Type + Svelte check
        working-directory: flowui
        run: npm run check

      - name: Build
        working-directory: flowui
        run: npm run build

  go:
    name: Go TUI (flowcli · vet · build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v5
        with:
          go-version-file: flowcli/go.mod

      - name: Vet
        working-directory: flowcli
        run: go vet ./...

      - name: Build
        working-directory: flowcli
        run: go build ./...
