# Demo build: keeps the in-memory fixture store instead of talking to the core.
# Override the server URL in dev with VITE_SERVER_URL when the core is elsewhere.
VITE_DEMO=1

# Deployed origin for the social-preview meta (absolute og:image/og:url).
VITE_SITE_URL=https://mbe24.github.io/flowcontrol