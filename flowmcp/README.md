# flowmcp — MCP server

TypeScript MCP server exposing the FlowControl core to agent hosts over the
Model Context Protocol. Talks to `flowd` from the same shared bindings as the
web app (`@flow/api`), so the wire contract stays in one place.

Stub — implementation to follow.
