# flowcontrol
Dependency-aware task graphs for humans and the agents working alongside them. Work packages hold tasks, tasks hold verifiable steps, and dependencies cross any level — mark one node DONE and the engine re-evaluates everything downstream. One data model, three front doors: web app, terminal UI, MCP server.
