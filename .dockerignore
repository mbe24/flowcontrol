# Build context for flowd/Dockerfile is the repo root (compose `context: .`).
# The Dockerfile has no COPY (source is bind-mounted at runtime), so this only
# trims the context transferred to the daemon on `--build`.
.git
node_modules
**/node_modules
flowd/target
target
flowui/dist
flowui/.svelte-kit
**/*.log
