//! flowd binary entry point.
//!
//! The full `flowd serve` subcommand (DB init + gRPC + web serving) lands with
//! the server work; this is a minimal binary so the crate compiles.

fn main() {
    println!("flowd: FlowControl core daemon (not yet wired).");
}
