# Local runner override for native-runner.mjs.
# Set to docker on hosts that cannot build/run native binaries, local on CI/dev
# boxes that can. Choices: docker | local | auto (default).
FLOWD_NATIVE_RUNNER=docker
